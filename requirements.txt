# Bénin Insights — dépendances core
# Installation : `make install` (crée .venv si nécessaire et installe ici).

# --- Données : extraction & stockage ---
google-cloud-bigquery>=3.20.0
google-cloud-bigquery-storage>=2.25.0
db-dtypes>=1.2.0  # requis par BigQuery pour la conversion en pandas DataFrame
pandas>=2.2.0
pyarrow>=15.0.0

# --- Configuration & utilitaires ---
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0

# --- Visualisation & dashboard ---
streamlit>=1.32.0
plotly>=5.18.0
matplotlib>=3.8.0
scipy>=1.12.0
seaborn>=0.13.0

# --- Notebooks ---
jupyterlab>=4.0.0
ipykernel>=6.29.0
nbconvert>=7.17.1
