"""Primitives analytiques pures (DataFrame -> DataFrame).

Cette couche ne dépend ni de modèles entraînés (`src/ml/`), ni de visualisation
(`src/viz/`), ni de questions de recherche (`src/questions/`). Elle est donc
réutilisable depuis n'importe quel orchestrateur (notebook, CLI, dashboard).
"""
